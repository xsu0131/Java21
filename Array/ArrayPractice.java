package Array;

import java.util.ArrayList;

public class ArrayPractice 
{
    // Java arrays are fixed in size (static)
    int[] arr = new int[5]; // static fixed size array

    // you can't change the length after creation.

    // in order to get more space, you need to make a new array

    int[] arr2 = new int[6];

    // Dynamic Arrays in Java

    ArrayList<Integer> list = new ArrayList<>();

    // Under the hood of ArrayList

}
